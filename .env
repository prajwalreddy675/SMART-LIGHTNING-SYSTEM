# Server Configuration
PORT=5000
JWT_SECRET=super_secret_lighting_hub_key

# Database Link (Leave empty to use the auto local JSON file-DB fallback)
# MONGODB_URI=mongodb://localhost:27017/smart_lighting
